// Number//
const age = 30;
console.log(age, 'age', typeof age);

// String //
const name = 'Alex';
console.log(name, 'name', typeof name);

// Boolean //
const hungry = true;
console.log(hungry, 'hungry', typeof hungry);

// Null //
const eyecolor = null;
console.log(eyecolor, 'eyercolor', typeof eyecolor);

// Undefined //
const athlete = undefined;
console.log(athlete, 'athlete', typeof athlete );

// Object//
const person = {
    name: 'Alex',
    age: 30,
    hungry: true,
};
console.log(person);

// bigint //
const  bigintNumber = BigInt(10);
console.log(bigintNumber, 'bigintNumber', typeof bigintNumber);

// Symbol //  
const id = Symbol('id');
console.log(id, 'id', typeof id);

